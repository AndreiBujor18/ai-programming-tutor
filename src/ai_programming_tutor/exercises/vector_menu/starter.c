#include <ctype.h>
#include <stdio.h>

/* Add small functions for reading the vector, its sum, and its maximum. */

int main(void) {
    int values[100] = {0};
    int item_count = 0;
    int has_values = 0;
    /* Read and dispatch the command sequence. */

    return 0;
}
