#include <ctype.h>
#include <stdio.h>

/* Add small functions for reading the matrix and finding each row maximum. */

int main(void) {
    int matrix[20][20] = {{0}};
    int row_count = 0;
    int column_count = 0;
    int has_matrix = 0;
    /* Read and dispatch the command sequence. */

    return 0;
}
