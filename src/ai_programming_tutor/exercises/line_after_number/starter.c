#include <stdio.h>
#include <string.h>

int main(void) {
    int identifier;
    scanf("%d", &identifier);

    /* Read the complete following line without keeping the pending newline. */

    return 0;
}
