#include <stdio.h>

int main(void) {
    int item_count;
    scanf("%d", &item_count);

    int values[1001] = {0};
    /* Read the vector and target, delete every match, and print the requested result. */

    return 0;
}
