{
  "id": "delete_occurrences",
  "title": "Delete all occurrences",
  "statement": "Delete every occurrence of a target integer from a vector. Report how many values were removed and print the remaining vector.",
  "input_format": "The first line contains n (1 <= n <= 1000). The second line contains n integers. The third line contains the target.",
  "output_format": "Print the removed count, the remaining count, and then every remaining value on one line.",
  "tags": ["arrays", "deletion", "shifting", "loops"]
}
