#include <stdio.h>

int main(void) {
    int item_count;
    scanf("%d", &item_count);

    int values[101] = {0};
    /* Read the vector, insert the value at the requested position, and print it. */

    return 0;
}
