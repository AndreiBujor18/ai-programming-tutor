#include <stdio.h>

int main(void) {
    long long lower_bound;
    long long upper_bound;
    scanf("%lld %lld", &lower_bound, &upper_bound);

    /* Find the number of integers in the closed interval and report its parity. */

    return 0;
}
